{
  "name": "cloudflare-dns-id-extension",
  "version": "1.0.0",
  "description": "Chrome extension to display DNS record IDs in Cloudflare dashboard",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.3.0"
  }
}
